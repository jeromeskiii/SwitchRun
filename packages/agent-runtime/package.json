{
  "name": "agent-runtime",
  "version": "0.1.0",
  "description": "Minimal, auditable local agent runtime with tool execution, session persistence, and MCTS/ADA engine integration",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && npm run build && node --test dist/tests/**/*.test.js",
    "start": "node dist/index.js"
  },
  "keywords": [
    "agent",
    "runtime",
    "mcts",
    "cli",
    "tools",
    "session"
  ],
  "author": "Human Systems",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "fast-glob": "^3.3.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
